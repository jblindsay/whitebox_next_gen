library(whiteboxworkflows)

# Minimal typed raster wrapper example.
# Replace "dem.tif" with a real raster path in your workspace.

# dem <- wbw_read_raster("dem.tif")
# print(dem)
#
# meta <- dem$metadata()
# str(meta)
#
# arr <- dem$to_array()
# str(arr)
#
# s <- dem$to_stars()
# str(s)